spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/plan_data
    username: postgres
    password: Xcx12345xc
    driver-class-name: org.postgresql.Driver


jpa:
  hibernate:
    ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

      logging:
        level:
          org.springframework: debug